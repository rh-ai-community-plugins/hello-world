apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-world-plugin.fullname" . }}
  labels:
    {{- include "hello-world-plugin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hello-world-plugin.selectorLabels" . | nindent 4 }}
